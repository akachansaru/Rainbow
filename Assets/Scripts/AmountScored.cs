using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Attached to each score text that shows up briefly when a cube is placed
public class AmountScored : MonoBehaviour {

	void Update () {
        transform.LookAt(Camera.main.transform);
	}
}
